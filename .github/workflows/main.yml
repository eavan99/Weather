name: Daily Weather Bot

on:
  schedule:
    # 每天北京时间早上 8:40 运行 (UTC 0:00)
    - cron: '40 0 * * *'
  workflow_dispatch: # 允许手动点击运行，方便测试

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run bot
        env:
          WEATHER_KEY: ${{ secrets.WEATHER_KEY }}
          CITY_ID: ${{ secrets.CITY_ID }}
          WXPUSHER_TOKEN: ${{ secrets.WXPUSHER_TOKEN }}  # 必须有这行
          WXPUSHER_UIDS: ${{ secrets.WXPUSHER_UIDS }}    # 必须有这行
        run: python weather_bot.py
